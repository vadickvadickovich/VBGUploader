with open('files.bbs') as f:            # ok, pretty good
   content = f.readlines()              # not bad too
   string.split(slitter)                # WHAT THE FUCK
   for line in content:                 # tabs are wrong, but nevermind
'files.bbs', tags = line.split("-")     # what is this i don't even

# conclusion: 2/10 - it won't even run... prepare your melon

# ok, i will write a little example for you, only once
